{
  "name": "zk-proof",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile_circuit": "mkdir -p output && pushd output && circom ../circuit.circom --r1cs --wasm --sym; popd",
    "snarkjs_constraints": "snarkjs rp output/circuit.r1cs output/circuit.sym",
    "snarkjs_info": "snarkjs info -r output/circuit.r1cs",
    "snarkjs_calculate_witness": "pushd output && snarkjs wc circuit.wasm ../input.json && snarkjs wej witness.wtns; popd"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "circom": "^0.5.46",
    "snarkjs": "^0.4.24"
  }
}
